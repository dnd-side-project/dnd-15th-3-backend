apiVersion: v1
kind: Service
metadata:
  name: momo-postgres
  labels:
    app.kubernetes.io/name: momo-postgres
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: momo-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
