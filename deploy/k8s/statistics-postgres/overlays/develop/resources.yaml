apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: momo-statistics-postgres
spec:
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        storageClassName: local-path
        resources:
          requests:
            storage: 5Gi
