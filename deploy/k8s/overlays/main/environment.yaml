apiVersion: apps/v1
kind: Deployment
metadata:
  name: momo-api
spec:
  template:
    spec:
      containers:
        - name: api
          env:
            - name: NODE_ENV
              value: production
            - name: CORS_ORIGINS
              value: "https://dnd-15th-3-frontend.pages.dev"
