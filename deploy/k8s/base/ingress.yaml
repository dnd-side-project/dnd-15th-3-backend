apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: momo-api
spec:
  ingressClassName: traefik
  rules:
    - host: placeholder.invalid
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: momo-api
                port:
                  number: 80
