name: Build and deploy image

on:
  push:
    branches: [main, develop]
    paths:
      - "src/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "nest-cli.json"
      - "tsconfig*.json"
      - "Dockerfile"
      - ".dockerignore"
      - ".github/workflows/image.yml"
  pull_request:
    branches: [main, develop]
    paths:
      - "src/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "nest-cli.json"
      - "tsconfig*.json"
      - "Dockerfile"
      - ".dockerignore"
      - ".github/workflows/image.yml"
  workflow_dispatch:

permissions:
  contents: write
  packages: write

concurrency:
  group: image-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  build-and-publish:
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v6

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: ${{ github.event_name != 'pull_request' }}
          platforms: linux/arm64
          tags: ghcr.io/dnd-side-project/dnd-15th-3-backend:${{ github.event_name == 'pull_request' && format('ci-{0}', github.sha) || format('{0}-{1}', github.ref_name, github.sha) }}
          labels: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Update the environment image tag
        if: github.event_name != 'pull_request'
        env:
          BRANCH: ${{ github.ref_name }}
          IMAGE_TAG: ${{ github.ref_name }}-${{ github.sha }}
        run: |
          case "$BRANCH" in
            develop) OVERLAY=develop ;;
            main) OVERLAY=main ;;
            *) echo "Unsupported deployment branch: $BRANCH" >&2; exit 1 ;;
          esac

          FILE="deploy/k8s/overlays/$OVERLAY/kustomization.yaml"
          sed -i -E "s#newTag: .*#newTag: $IMAGE_TAG#" "$FILE"

          git diff --exit-code || true
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$FILE"
          git diff --cached --quiet && exit 0
          git commit -m "chore(deploy): update $OVERLAY image to $IMAGE_TAG"
          git push origin "HEAD:$BRANCH"
